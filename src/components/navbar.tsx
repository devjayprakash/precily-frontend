import * as React from "react";

const Navbar: React.FC = () => {
  return (
    <div className={"w-full bg-purple-500 text-white"}>
      <div className={"px-3 py-2"}>Precily frontend</div>
    </div>
  );
};

export default Navbar;
